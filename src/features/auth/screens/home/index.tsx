import React from "react";
import HomeContainer from "@features/auth/containers/home";

const HomeScreen = () => {
  return <HomeContainer onCreatedAccount={() => null} onLogin={() => null} />;
};

export default HomeScreen;
